<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <style>

       .container_login{
        width: 100%;
        margin-top: 2rem;
        padding: 4rem;
        display: flex;
        justify-content: center;
       }
       form{
        background: rgba(81, 219, 70, 0.1);
        border-radius: 7%;
        width: 45rem;
        aspect-ratio: 1;
        padding: 4rem;
        display: flex;
        align-items: center;
        flex-direction: column;
        gap: 4rem;
        & div{
            display: flex;
            flex-direction: column;
        }
       }
       label{
        font-size: 2.4rem;
        margin-bottom: 1rem;
       }
       input{
        font-size: 1.8rem;
        padding: .7rem 7rem .7rem .7rem;
       }
       button{
        width: 14rem;
        margin-top: 4rem;
        font-size: 2.4rem;
        padding: .5rem;
        letter-spacing: .4rem;
       }


    </style>
</head>
<body>
   <%- include("header") %>
   <div class="container_login">
    <form action="/login_submit" method="post" >
        <div class="username_div">

            <label for="username">username or email</label>
            <input type="text" name="username" autocomplete="off" autofocus placeholder="username or email" value="<%- username %>" >
        </div>

        <div class="user_password">
            <label for="password">password</label>
            <input type="text" name="password" placeholder="password" value="<%= password %>">
        </div>
        <button type="submit">login</button>
    </form>
   </div>
</body>
</html>